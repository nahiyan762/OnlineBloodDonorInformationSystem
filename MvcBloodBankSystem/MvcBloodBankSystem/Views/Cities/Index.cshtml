@model IEnumerable<BloodBankModels.City>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout1.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.city_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.city_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.city_id }, htmlAttributes: new { @class = "btn btn-success" }) |
            @Html.ActionLink("Details", "Details", new { id=item.city_id }, htmlAttributes: new { @class = "btn btn-info" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.city_id }, htmlAttributes: new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
<p>
    @Html.ActionLink("Back", "Index", "WelcomeAdmin")
</p>
